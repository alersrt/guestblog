<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/data/mongo
                            http://www.springframework.org/schema/data/mongo/spring-mongo.xsd
                            http://www.springframework.org/schema/beans
                            http://www.springframework.org/schema/beans/spring-beans.xsd">

    <mongo:template db-factory-ref="mongoDbFactory" converter-ref="mappingConverter"/>

    <mongo:mapping-converter>
        <mongo:custom-converters base-package="org.student.guestblog.converters">
        </mongo:custom-converters>
    </mongo:mapping-converter>

    <mongo:repositories base-package="org.student.guestblog.dao"/>

    <!--Multipart resolver-->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="16777216" />
    </bean>

    <!-- MongoDB GridFS Template -->
    <bean id="gridFsTemplate" class="org.springframework.data.mongodb.gridfs.GridFsTemplate">
        <constructor-arg ref="mongoDbFactory"/>
        <constructor-arg ref="mappingConverter"/>
    </bean>

    <!--Validator-->
    <bean id="localValidatorFactoryBean" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean"/>

    <bean id="validatingMongoEventListener" class="org.springframework.data.mongodb.core.mapping.event.ValidatingMongoEventListener">
        <constructor-arg ref="localValidatorFactoryBean"/>
    </bean>

    <!--Database profiles-->
    <beans profile="master">
        <context:property-placeholder location="classpath:db/database.properties"/>
        <mongo:db-factory uri="${mongo.uri}" />
    </beans>

    <beans profile="dev">
        <context:property-placeholder location="classpath:db/dev_database.properties"/>
        <mongo:db-factory uri="${mongo.uri}" />
    </beans>
</beans>